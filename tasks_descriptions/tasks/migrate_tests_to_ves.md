# Task: Миграция тестов на VES и исправление песочниц

## Контекст
Нам нужно исправить `tests/test_environment_level.py`, используя новую концепцию изоляции.

## Шаги реализации
1.  **Sandbox Update**:
    - [ ] Обновить фикстуру `hsm_sandbox`, чтобы она поддерживала новую структуру реестра.
2.  **Test Migration**:
    - [ ] Переписать `test_case_1_implication_merging` с использованием `runtime: uv`.
    - [ ] Переписать остальные кейсы под новую модель `ServiceManifest`.
3.  **Validation**:
    - [ ] Убедиться, что тесты проходят с `--basetemp=./debug_tests` без загрязнения основного проекта.

## Критерии готовности
- [ ] Все тесты в `tests/test_environment_level.py` проходят успешно.
- [ ] В корневом `pyproject.toml` не появляется лишних записей после прогона тестов.

## Отчет о реализации (Завершено 2026-02-08)
- **Что сделано**: Полная миграция тестовой базы на архитектуру VES, внедрение CLI для управления импликациями и исправление иерархии режимов.
- **Как реализовано**:
    - **Infrastructure**: Обновлена фикстура `hsm_sandbox` для поддержки новой структуры реестра (`libraries`, `services`).
    - **Implies CLI**: Реализован полноценный интерфейс `hsm registry <library|service|group option> implies add/remove`. Это позволило полностью отказаться от ручной правки YAML в тестах.
    - **Modes Logic**: Упрощена логика режимов в `manifest.py`. Теперь `hsm.yaml` — это атомарный снимок намерений пользователя, где режим хранится на уровне компонента. Команды `hsm mode` (для всего проекта) и переключение режима через группы в `hsm library/service mode <group_name>` работают как инструменты массового обновления манифеста.
    - **Test Migration**:
        - `tests/test_environment_level.py` переписан на использование CLI и паттерна **Local-Remote** (локальные git-репозитории) для исключения сетевых запросов.
        - Исправлены все тесты, использующие устаревшие команды `package` и `container`.
        - Внедрена поддержка `Implication Merging` для сервисов и групп сервисов в `SyncEngine`.
    - **Bugfixes**: Исправлены опечатки в путях реестра (`service_groups`) и логика сбора импликаций в `SyncEngine`.
- **Результат**: Все 31 тест (включая High-Fidelity, CLI и Core) проходят успешно. Система гарантирует изоляцию и корректную работу связей между кодом и инфраструктурой.