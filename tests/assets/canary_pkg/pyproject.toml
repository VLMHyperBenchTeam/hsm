[project]
name = "canary-pkg"
version = "0.1.0"
description = "A canary package that fails to build if HSM_CANARY_VAR is missing"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.hooks.custom]
path = "hatch_build.py"
